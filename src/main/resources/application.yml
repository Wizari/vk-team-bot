server:
  port: 9222
  servlet:
    context-path: /

spring:
  application:
    name: vk-team-bot

vk-team-bot:
  token: "xxx2" # Токен сообщества VK Team
  chat-id: "xxx"

logging:
  level:
    com.example.vkteambot: DEBUG